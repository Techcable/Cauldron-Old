--- ../src-base/minecraft/net/minecraft/entity/passive/EntityPig.java
+++ ../src-work/minecraft/net/minecraft/entity/passive/EntityPig.java
@@ -22,6 +22,11 @@
 import net.minecraft.stats.AchievementList;
 import net.minecraft.world.World;
 
+// CraftBukkit start
+import org.bukkit.craftbukkit.event.CraftEventFactory;
+import org.bukkit.craftbukkit.util.CraftMagicNumbers;
+// CraftBukkit end
+
 public class EntityPig extends EntityAnimal
 {
     // JAVADOC FIELD $$ field_82184_d
@@ -139,24 +144,29 @@
     // JAVADOC METHOD $$ func_70628_a
     protected void dropFewItems(boolean par1, int par2)
     {
+        // CraftBukkit start
+        java.util.List<org.bukkit.inventory.ItemStack> loot = new java.util.ArrayList<org.bukkit.inventory.ItemStack>();
         int j = this.rand.nextInt(3) + 1 + this.rand.nextInt(1 + par2);
 
-        for (int k = 0; k < j; ++k)
+        if (j > 0)
         {
             if (this.isBurning())
             {
-                this.dropItem(Items.cooked_porkchop, 1);
+                loot.add(new org.bukkit.inventory.ItemStack(CraftMagicNumbers.getMaterial(Items.cooked_porkchop), j));
             }
             else
             {
-                this.dropItem(Items.porkchop, 1);
+                loot.add(new org.bukkit.inventory.ItemStack(CraftMagicNumbers.getMaterial(Items.porkchop), j));
             }
         }
 
         if (this.getSaddled())
         {
-            this.dropItem(Items.saddle, 1);
+            loot.add(new org.bukkit.inventory.ItemStack(CraftMagicNumbers.getMaterial(Items.saddle), 1));
         }
+
+        CraftEventFactory.callEntityDeathEvent(this, loot);
+        // CraftBukkit end
     }
 
     // JAVADOC METHOD $$ func_70901_n
@@ -184,9 +194,23 @@
         if (!this.worldObj.isRemote)
         {
             EntityPigZombie entitypigzombie = new EntityPigZombie(this.worldObj);
+
+            // MCPC+ start
+            if (par1EntityLightningBolt != null)
+            {
+                // CraftBukkit start
+                if (CraftEventFactory.callPigZapEvent(this, par1EntityLightningBolt, entitypigzombie).isCancelled())
+                {
+                    return;
+                }
+
+                // CraftBukkit end
+            }
+            // MCPC+ end
             entitypigzombie.setCurrentItemOrArmor(0, new ItemStack(Items.golden_sword));
             entitypigzombie.setLocationAndAngles(this.posX, this.posY, this.posZ, this.rotationYaw, this.rotationPitch);
-            this.worldObj.spawnEntityInWorld(entitypigzombie);
+            // CraftBukkit - added a reason for spawning this creature
+            this.worldObj.addEntity(entitypigzombie, org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.LIGHTNING);
             this.setDead();
         }
     }
